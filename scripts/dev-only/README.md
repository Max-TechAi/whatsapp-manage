# Dev-only scripts

Local debugging utilities — not used in production builds or Docker images.

- `check-preview.js` — print recent chat preview rows from Postgres
- `test-db.js` — probe common local `DATABASE_URL` variants
